spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
security.token.secret=${SPRING_TOKEN_SECRET}
security.tokencandidate.secret=${SPRING_TOKENCANDIDATE_SECRET}
aws.access-key=${SPRING_AWS_ACCESS_KEY}
aws.secret-key=${SPRING_AWS_SECRET_KEY}
aws.s3.bucket-name=${SPRING_AWS_BUCKET_NAME}
aws.region=${SPRING_AWS_REGION}
